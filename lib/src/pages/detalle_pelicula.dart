import 'package:flutter/material.dart';

class DetallePeliculaPage extends StatelessWidget {
  const DetallePeliculaPage({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("DetallePeliculaPage"),
    );
  }
}